# Leg P — Pre-registration: multi-messenger no-hair test on real data (backlog B2)

*ansatz §93's no-hair null test: a Kerr hole's spin read three ways (EHT shadow, X-ray ISCO, LIGO ringdown)
must agree. Confront it with real multi-messenger data. SCOPE (logged): ingests external EHT/X-ray data,
relaxing the strict 3-siblings rule for this forward-looking leg.*

## Method
Tabulate the three real spin measurements (cited); apply ansatz's exact §93 maps; cross-check the ringdown
map on deepstrain's GW250114; forecast the test's power from ansatz's deformation sensitivity (spread of the
three inferred spins vs deformation ε) against measurement precisions.

## Frozen expectations (no outcome assumed)
- The three messengers measure DIFFERENT objects (disjoint masses) → the per-object consistency null may not
  be runnable on current data; if so, that data-gap IS the finding.
- The forecast: spread(ε) sets the detectable deformation given a spin precision. Whether current/future
  precision suffices is to be computed, not assumed.
- The ringdown map must reproduce deepstrain's GW250114 spin (gate; = Move B v2).

## Discipline
ansatz §93 maps read-only; deepstrain ringdown read-only; external EHT/X-ray values cited in FINDINGS.

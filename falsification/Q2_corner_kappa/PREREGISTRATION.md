# Q2 — Pre-registration: independent reimplementation of quantum's κ-vs-corner result

*Frozen 2026-08-22 before `code/` is written. Requested by quantum (vestigium). **Their code is not read
and will not be.** They supplied system, regulators, question, resolutions and agreement criteria — and
deliberately **no extraction method, no fit model, and no numbers.***

## Why this exists

quantum: *"vestigium has zero second implementations of anything. Every headline rests on one codebase.
This is the only result nobody has ever checked, and the one I would most like to be wrong about early
rather than late."*

Last night we ran a blind test of their corner-coefficient **extraction** and agreed to the digits. The
value turned out to be in a caveat: **blindness removed the implementation correlation between their
generator and their fitter, but not the MODEL correlation** — S was generated by exactly the three terms
the fit used. They then found a 1% unmodelled term moves the coefficient by 7%, and amended a published
number. **This run exists to avoid repeating that**: I choose my own extraction.

## The claim under test — theirs, stated before my numbers exist

> **The CORNER spread falls under lattice refinement while the AREA spread does not.**

Not the single-resolution value. That is the model-dependent half, and it is the half that already moved
once.

## Frozen gates

- **Q2a — the regulators must agree at small k (run FIRST).** All four must match `m² + k²` to O(k⁴) as
  k→0. quantum: *"if one does not in your hands, that disagreement is the first finding and we should stop
  there."* **If Q2a fails, nothing below is run.**
- **Q2b — POSITIVE CONTROL, and it is a control that must FAIL to be universal.** The area coefficient is
  genuinely regulator-dependent, so **the area spread must come out LARGE**. quantum names `< 10%` as the
  condition under which *I* am wrong rather than them. A correct extraction cannot make a
  regulator-dependent quantity universal. **This is free and it runs before the headline is read.**
- **Q2c — PRIMARY, the claim.** Agreement iff **both**:
  - corner spread falls by **≥ 3×** from s=1 to s=2, **and**
  - area spread changes by **< 10% relative** between s=1 and s=2.
  Disagreement iff the corner spread is flat or rising, **or** the area spread falls comparably to the
  corner one.
- **Q2d — magnitudes, deliberately looser.** Area spread in **25–50%** at both resolutions; corner spread
  **< 5%** at s=1. quantum has stated in advance they will **not** treat a factor-of-two difference in the
  corner spread as disagreement, because their own number moves that much between two legitimate fit
  models.
- **Q2e — MY extraction must be shown not to be the answer.** Since I choose the fit, I must report the
  corner spread under **at least two different fit models** (e.g. with and without a subleading term) and
  state how much it moves. *If my headline conclusion depends on which model I picked, I have reproduced
  the exact defect this exercise exists to avoid, and I report that instead of a spread.*

## Declared in advance

- **Protocol: my numbers to quantum FIRST**, before I see any of theirs. Same as the corner test.
- **They disclosed — without describing it — that they have since run a further study on a different
  lattice geometry bearing on the same claim**, and are withholding it to avoid anchoring me. Noted here
  so it is on record that the withholding was deliberate and declared, not discovered later.
- **s=3 (L=480, m=0.00333, l=12,18,…,60) is NEW INFORMATION FOR BOTH OF US** if I run it — they confirmed
  s=1 and s=2 are the whole of their refinement axis. If affordable, run it; a third point tests whether
  the trend has structure two points cannot show. **Reported separately from the s=1/s=2 comparison that
  Q2c gates on**, so it cannot contaminate the frozen test.
- **What would make me conclude MY implementation is wrong:** Q2a fails; or Q2b returns a small area
  spread; or my area spreads disagree with theirs while my corner behaviour matches, which would mean my
  machinery differs on the *easy* quantity.
- **Cost is bounded:** L=320, region side ≤ 40 ⇒ dense matrices at most 1600×1600. Correlators built by
  FFT on the L×L lattice, not by direct k-summation per pair.
